name: mesher
description: "Offline P2P mesh messenger — Bluetooth Low Energy."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  # State management
  flutter_riverpod: ^2.6.1

  # Local database
  drift: ^2.23.1
  drift_flutter: ^0.2.4
  sqlite3_flutter_libs: ^0.5.26

  # Persistent key/value storage for node ID, nickname, avatar
  flutter_secure_storage: ^9.2.2

  # Foreground service (BLE scan в фоне)
  flutter_foreground_task: ^8.13.0

  # Packet codec
  cbor: ^6.3.1

  # Utilities
  uuid: ^4.5.1

  # Local notifications (новые сообщения, входящие звонки)
  flutter_local_notifications: ^17.2.4

  # File picker (выбор аватара)
  file_picker: ^8.1.4

  # Hashing (ключи, подписи)
  crypto: ^3.0.6

  # Pure-Dart image decode/resize/encode (avatar thumbnail)
  image: ^4.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.13
  drift_dev: ^2.23.1

flutter:
  uses-material-design: true

  # fonts:  # Раскомментировать после добавления TTF в assets/fonts/
  #   - family: SpaceGrotesk
  #     fonts:
  #       - asset: assets/fonts/SpaceGrotesk-Regular.ttf
  #       - asset: assets/fonts/SpaceGrotesk-SemiBold.ttf
  #         weight: 600
  #   - family: JetBrainsMono
  #     fonts:
  #       - asset: assets/fonts/JetBrainsMono-Regular.ttf
